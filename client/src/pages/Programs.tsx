import { ArrowRight, BookOpen, Clock3, Search, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { Link, useLocation } from "wouter";
import SiteShell from "@/components/SiteShell";
import { trpc } from "@/lib/trpc";

export default function Programs() {
  const [, setLocation] = useLocation();
  const [search, setSearch] = useState(new URLSearchParams(window.location.search).get("q") ?? "");
  const { data: programs, isLoading, isError } = trpc.catalog.listPrograms.useQuery({ search });
  function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setLocation(search.trim() ? `/programs?q=${encodeURIComponent(search.trim())}` : "/programs"); }
  return <SiteShell><section className="border-b border-wine/10 bg-canvas"><div className="mx-auto max-w-[1440px] px-5 py-16 sm:px-8 lg:px-12 lg:py-20"><p className="eyebrow">Certificate programs</p><div className="mt-4 flex flex-col justify-between gap-8 lg:flex-row lg:items-end"><div><h1 className="max-w-3xl font-serif text-5xl leading-none tracking-[-0.04em] text-ink sm:text-6xl">Focused learning with a verifiable finish.</h1><p className="mt-5 max-w-2xl leading-7 text-ink/70">Explore current NIU certificate programs. Availability, requirements, and learning paths are published by authorised academic staff.</p></div><form onSubmit={submit} className="flex w-full max-w-md items-center gap-2 border-b-2 border-wine bg-white px-3 py-3 shadow-sm"><Search className="h-5 w-5 text-wine" /><input value={search} onChange={(event) => setSearch(event.target.value)} aria-label="Search certificate programs" placeholder="Search by name or code" className="min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-ink/40" /><button className="text-sm font-bold text-wine">Search</button></form></div></div></section><section className="mx-auto max-w-[1440px] px-5 py-14 sm:px-8 lg:px-12 lg:py-18"><div className="mb-8 flex items-center gap-2 text-xs text-ink/60"><ShieldCheck className="h-4 w-4 text-wine" />Only published programmes appear here. NIU does not currently offer degree enrolment.</div>{isLoading && <div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{[1,2,3].map((item) => <div key={item} className="h-64 animate-pulse bg-canvas" />)}</div>}{isError && <div className="border-l-4 border-wine bg-wine/5 p-6"><h2 className="font-serif text-2xl">Programme discovery is temporarily unavailable.</h2><p className="mt-2 text-sm text-ink/70">Please retry shortly or contact NIU support if the issue continues.</p></div>}{!isLoading && !isError && programs?.length === 0 && <div className="border border-dashed border-wine/25 bg-canvas p-10 text-center"><BookOpen className="mx-auto h-8 w-8 text-wine" /><h2 className="mt-5 font-serif text-3xl text-ink">No published certificate programs yet.</h2><p className="mx-auto mt-3 max-w-lg text-sm leading-6 text-ink/65">The catalogue is populated only from authorised NIU academic records. Check back once a programme is published.</p><Link href="/contact" className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-wine">Contact NIU support <ArrowRight className="h-4 w-4" /></Link></div>}{!isLoading && !isError && programs && programs.length > 0 && <div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{programs.map((program) => <article key={program.id} className="flex min-h-72 flex-col border border-wine/10 bg-white p-6 shadow-[0_16px_36px_rgba(29,25,21,0.05)]"><p className="text-xs font-bold uppercase tracking-[0.16em] text-wine">{program.code}</p><h2 className="mt-4 font-serif text-3xl leading-tight text-ink">{program.name}</h2><p className="mt-4 line-clamp-3 text-sm leading-6 text-ink/65">{program.description}</p><div className="mt-auto flex items-center justify-between border-t border-wine/10 pt-5 text-xs text-ink/60"><span className="flex items-center gap-1.5"><Clock3 className="h-3.5 w-3.5" />{program.duration_hours} learning hours</span><span className="capitalize">{program.difficulty}</span></div></article>)}</div>}</section></SiteShell>;
}
