import { ArrowRight, CircleAlert, LockKeyhole, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";
import SiteShell from "@/components/SiteShell";
import { supabase, supabaseConfigured } from "@/lib/supabase";

export default function SignIn() { const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(false); async function signInWithGoogle() { setError(null); if (!supabaseConfigured) { setError("The NIU authentication connection has not been configured yet."); return; } setLoading(true); const { error: signInError } = await supabase.auth.signInWithOAuth({ provider: "google", options: { redirectTo: `${window.location.origin}/portal` } }); if (signInError) { setError(signInError.message); setLoading(false); } } return <SiteShell><section className="mx-auto grid min-h-[calc(100vh-74px)] max-w-[1440px] items-stretch lg:grid-cols-[.95fr_1.05fr]"><div className="bg-wine px-5 py-14 text-paper sm:px-8 lg:flex lg:flex-col lg:justify-between lg:px-12 lg:py-16"><div><p className="eyebrow text-gold">NIU account access</p><h1 className="mt-5 max-w-md font-serif text-5xl leading-none tracking-[-0.04em]">One learning record. Clear permissions.</h1><p className="mt-6 max-w-md leading-7 text-paper/75">Students, faculty, registrar staff, and authorised administrators receive only the access appropriate to their official role.</p></div><div className="mt-14 grid gap-4"><div className="flex gap-3"><ShieldCheck className="h-5 w-5 shrink-0 text-gold" /><p className="text-sm text-paper/75">Google account authentication is supported through NIU’s configured identity provider.</p></div><div className="flex gap-3"><LockKeyhole className="h-5 w-5 shrink-0 text-gold" /><p className="text-sm text-paper/75">Administrator access requires an approved allowlist entry and is never granted automatically.</p></div></div></div><div className="flex items-center px-5 py-14 sm:px-8 lg:px-16"><div className="w-full max-w-md"><h2 className="font-serif text-4xl tracking-[-0.03em] text-ink">Sign in to NIU</h2><p className="mt-3 leading-6 text-ink/65">Use your approved NIU-linked Google account to continue to the learning platform.</p>{error && <div className="mt-6 flex gap-3 border-l-4 border-wine bg-wine/5 p-4 text-sm text-ink/75"><CircleAlert className="h-5 w-5 shrink-0 text-wine" />{error}</div>}<button onClick={signInWithGoogle} disabled={loading} className="mt-8 flex w-full items-center justify-center gap-3 border border-wine/25 bg-white px-5 py-4 text-sm font-bold text-ink shadow-sm transition hover:border-wine disabled:cursor-not-allowed disabled:opacity-70"><span className="grid h-5 w-5 place-items-center rounded-full bg-[#4285F4] text-[10px] font-bold text-white">G</span>{loading ? "Redirecting to Google…" : "Continue with Google"}<ArrowRight className="h-4 w-4 text-wine" /></button><p className="mt-6 text-xs leading-5 text-ink/55">By continuing, you acknowledge that NIU access is subject to applicable institutional policies and account authorization.</p><Link href="/help" className="mt-5 inline-block text-sm font-semibold text-wine hover:text-ink">Need support with account access?</Link></div></div></section></SiteShell>; }
